import { Activity, AlertCircle, FileText, User } from "lucide-react";
import { Card } from "@/components/ui/card";
import { useNavigate } from "react-router-dom";

const Dashboard = () => {
  const navigate = useNavigate();

  const actions = [
    {
      icon: Activity,
      title: "Scan Symptoms",
      description: "AI-powered symptom analysis",
      variant: "primary" as const,
      onClick: () => navigate("/symptom-scanner"),
    },
    {
      icon: FileText,
      title: "Health Records",
      description: "Your medical history",
      variant: "secondary" as const,
      onClick: () => navigate("/health-records"),
    },
    {
      icon: AlertCircle,
      title: "Emergency SOS",
      description: "Quick emergency assistance",
      variant: "emergency" as const,
      onClick: () => navigate("/emergency-sos"),
    },
    {
      icon: User,
      title: "My Profile",
      description: "Manage your information",
      variant: "default" as const,
      onClick: () => navigate("/profile"),
    },
  ];

  return (
    <div className="grid grid-cols-1 md:grid-cols-2 gap-4 w-full max-w-4xl">
      {actions.map((action) => (
        <Card
          key={action.title}
          className={`dashboard-card dashboard-card-${action.variant} cursor-pointer group`}
          onClick={action.onClick}
        >
          <div className="flex flex-col items-center text-center p-6 space-y-4">
            <div className="dashboard-icon-wrapper">
              <action.icon className="w-8 h-8" />
            </div>
            <div>
              <h3 className="text-xl font-semibold mb-1">{action.title}</h3>
              <p className="text-sm opacity-80">{action.description}</p>
            </div>
          </div>
        </Card>
      ))}
    </div>
  );
};

export default Dashboard;
